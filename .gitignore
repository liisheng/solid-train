.venv/
.cache/
# Corpus, samples, and built tokenizer artifacts. `data/raw/` and `data/processed/` alone
# missed data/tokenizer_sample/ and data/tokenizer_final/, leaving 4.3 GB untracked but
# committable by a stray `git add -A`. Small evidence lives in docs/evidence/ instead.
data/
checkpoints/
runs/
__pycache__/
.pytest_cache/
.hypothesis/
*.pyc
.kiro/
tmp_*.log